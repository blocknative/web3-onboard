{
  "name": "web3-onboard-monorepo",
  "private": true,
  "workspaces": [
    "./packages/*"
  ],
  "scripts": {
    "format": "prettier --write 'packages/**/*.ts'",
    "dev": "yarn wsrun dev"
  },
  "devDependencies": {
    "prettier": "^2.4.1",
    "prettier-plugin-svelte": "^2.4.0",
    "typescript": "^4.5.5"
  }
}
