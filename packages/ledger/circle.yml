jobs:
  publish-ledger:
    docker:
      - image: cimg/node:16.13.1
    steps:
      - checkout
      - restore_cache:
          keys:
            - v2-dependencies-{{ checksum "package.json" }}
            - v2-dependencies-
      - run: yarn
      - save_cache:
          paths:
            - node_modules
          key: v2-dependencies-{{ checksum "package.json" }}
      - run: yarn build
      - run: cd packages/ledger && echo "//registry.npmjs.org/:_authToken=$npm_TOKEN" > .npmrc
      - run: cd packages/ledger && npm publish --access public

workflows:
  ledger:
    jobs:
      - publish-ledger:
          filters:
            branches:
              only:
                - v2-web3-onboard
